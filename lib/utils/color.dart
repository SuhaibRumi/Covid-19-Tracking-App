import 'package:flutter/material.dart';

// Global Colors
const kCardColor = Color(0xff282A3A);
const kBGColor = Color(0xff434242);
const kDynamic = Color(0xffF7CCAC);



// Global Cases Colors
const todayCase =  Colors.blue,
    totalCase = Colors.red,
    totalRecovered = Colors.green,
    totalDeaths = Colors.grey,
    activeCases = Colors.orange,
    criticalCases = Colors.purple,
    todayRecovered = Colors.teal,
    testCases = Colors.yellow;

    // GolobalDataListColors
     Color cases = Colors.blueAccent.withAlpha(500);
     Color deaths = Colors.red.withAlpha(500);
     Color recoveries = Colors.green.withAlpha(500);
     
    //  GlobalListTile
    Color whiteColor = Colors.white;
    Color shadowColor = Colors.grey.withAlpha(500);

    // themeColors
   Color primaryColor = Colors.blue;
   Color secondaryColor =  Colors.grey.withAlpha(100);

    
    // Fonts Family
    String primaryFont = 'MyFont';
    String secondaryFont = 'Roboto Mono';
